# ZenCube - Minimal Requirements (GUI Only)
# Use this for running just the GUI without ML features
# Install with: pip install -r requirements-minimal.txt

# Core GUI Framework
PySide6>=6.5.0

# System monitoring
psutil>=5.9.0

# Plotting for monitoring dashboard
matplotlib>=3.7.0
numpy>=1.24.0

# Prometheus metrics (optional)
prometheus-client>=0.17.0

# Note: For full ML features, use requirements.txt instead
# Total install size: ~500 MB (vs ~8 GB with torch/CUDA)
